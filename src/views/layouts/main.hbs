<!DOCTYPE html>

<html lang="en">
    <head>
        <title>{{title}}</title>
        {{> head}}
    </head>
    <body>
        {{> header}}

        <h1>{{title}}</h1>
        {{{body}}}

        {{> footer}}
    </body>
</html>